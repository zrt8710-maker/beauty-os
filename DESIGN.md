# Beauty OS — 场景中的个人记录

沿用用户确认的 public/brand/background.png、奶油白、雾紫、浅杏与暖灰紫文字。色值与字体以 src/app/globals.css 的现有变量为准。

## 本轮设计约定
- 首页与我的资产保持现有布局。
- 长期档案使用开放的编辑式阅读布局：基线、区域、反复问题与长期关注；用文字、留白和细线分组，不用大卡片嵌套小卡片。
- 今日护理保留内容顺序与操作，以连续步骤线组织护理步骤；辅助内容直接排列在背景上。
- 登录页为背景中的单一焦点，不再使用大白色矩形面板；输入和主要按钮保留明确边界。
- Focus Traveller 用户提供的截图仅作为场景与前景关系、集中操作、渐进展开的参考，不借用配色或插画。截图无法证明其实际动画参数。
- 不生成皮肤评分、诊断、虚构趋势或人物形象。保留所有用户自述、护理依据和风险说明。

## 交互
档案基线字段标签为 15px、值为 20px，避免数据展示字号压过章节标题；桌面编辑按钮与基线并排，不占独立空行。护理判断使用 18px 短标题与 16px 正文；依据分栏，未加入的护理按需展开。区域字段标签与正文统一为 15px，长期关注正文同为 15px，次要说明为 13px；保留双栏，观察区上下间距 24px、列表条目上下间距 12px。首页输入采用比 PM 标签底色更浅的淡紫色（#f7f5fa），默认单行输入加底部操作栏，示例文字位于整体中间偏上，多行输入按需增高。

加载反馈复用原品牌图中的标志，轻微呼吸仅在实际等待期间出现（回复、保存、生成方案、路由加载）。减少动画模式保持静止，不给静态首页水印添加持续动画。

延续 --ease-out、--duration-state、--duration-detail。展开反馈克制；高频操作立即响应。减少动画模式关闭新增动画。键盘焦点清晰，输入有标签，错误就近展示。沿用认证与保存接口。

## 验证
桌面与 320/375/414/768px 宽度检查；编辑/取消、展开/收起及登录错误状态检查。预览数据必须标明为模拟数据，不把预览冒充真实账户页面。


皮肤日记顶部采用日期、轻量记录状态与操作按钮同组，摘要紧随其后；首段不额外增加顶部 padding，和历史区之间只保留一条分隔线。

动效：时段底座跟随导航移动 220ms；新生成方案判断先出现，步骤按 60/100/140ms 间隔进入，沿用或保留旧方案不重播。消息仅挂载时淡入并上移 6px，发送按钮以 140ms 过渡可用状态。减少动态效果时取消位移和分步动画。
登录页保持全部居中；邮箱标签仅供读屏，表单紧接标题。背景柔光只在精细指针下轻微跟随，焦点加强光感；按钮具有方向提示、发送中信封和成功确认。邮箱错误仅在离焦或提交时提示，减少动态效果设置关闭位移。
最新登录页约束：背景是固定底图，不做视差、鼠标跟随或动态光晕；仅品牌与标题允许轻微平移，表单固定，聚焦输入时文字归位。
