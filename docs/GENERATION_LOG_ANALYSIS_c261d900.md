# 生成日志 c261d900 与解释布局修复

用户提供的日志总生成耗时 60495ms。分阶段约为：数据准备 9.6s，方案模型 33.42s，解释模型 15.03s，持久化 2.41s。模型阶段合计约 80%；并行查询及嵌套计时不能重复相加。输入 10929 tokens、输出 1917 tokens，仅此一次样本不能判断供应商排队或稳定吞吐。

## 本次修改

- 护理依据区域移除两列网格，改为最大 78ch 的连续单列，段落间距 16px。原布局右侧长解释撑高整行，把下一行的“为什么更偏向它”推远；本次没有缩短、隐藏或改写生成文字。
- 生成入口让资产查询与档案查询并行；档案确定时区后，旧方案、当日皮肤与天气三项并行。全部数据就绪后才进入原有决策、安全校验和保存流程。
- 按该样本、假设各查询耗时不变，初始读取关键路径从约 4.57s 变为约 3.24s，理论节省约 1.33s。这不是实际优化后测速，不应许诺线上固定提速。

## 验证

- 263 项方案、安全、今日展示和 API 测试通过。
- 新增受控等待测试验证并行起点、使用档案时区、每类数据只查一次且数据不完整时不保存。
- 生产构建（含 TypeScript）通过，相关文件 ESLint 通过。
- 使用实际 TodayRoutine 组件和现有测试数据生成静态排版预览，确认单列标记；真实浏览器截图未成功生成，尚未完成宽窄屏视觉验收。预览为 output/reasons-layout-preview.html，不包含真实用户数据。

## 进一步加速的边界

- 研究草稿批次读取在此样本中约 4.74s，应测行数、传输体积、查询执行计划和服务间网络后优化。当前选择有效草稿有版本及质量逻辑，不能直接截断到最新一行来换速度。
- 模型决策与解释有先后依赖。保留同一模型、上下文和验证逻辑时，需实测模型端排队、生成和缓存情况才能制定更大的提速方案；当前日志不能区分这些因素。
- 可以另行设计“已校验方案步骤先显示，解释随后完成”的渐进展示，改善等待感，但这不缩短模型总耗时，也需要处理保存、失败和交互一致性。本次未改此业务流程。
- 未修改模型、提示词、token 限制、产品候选数量、安全规则；未部署。
