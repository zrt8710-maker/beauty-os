# 安全清理记录

本次以当前工作区为基准检查引用；没有回退已有未提交修改。

## 已清理

- 删除 `HomeTodayTask`：当前首页已采用会话与提醒入口，源码没有导入该组件。同步删除仅覆盖这个旧组件的 5 项测试。
- 删除 `EnvironmentSummary`：当前侧栏使用 `SidebarEnvironmentEntry` 与 `WeatherLocationSettings`，源码没有导入旧天气栏。导航契约测试改为检查现用城市设置入口；天气刷新服务及其测试保留。
- 删除 OpenAI 对话 provider 内未被调用的 `normalizeContinuingDailyState`。另一个 provider 中仍被调用的同名容错函数保持原样。
- 删除会话界面未使用的导入、未使用的参数解构，以及日解读 provider 的未使用类型导入。保留现有组件参数兼容性。
- 使用反馈页直接将已按当前用户读取的完整方案传入既有 `toRoutine` 校验转换，去掉第二次查询、无用仓库初始化和仅为查询拼装的规则引擎空依赖。新增 4 项测试覆盖单次查询、时段查询、无权限或不存在的方案、未登录跳转。
- ESLint 忽略本地浏览器资料、截图工具输出及生成产物目录，不关闭任何源码检查规则。全库提示从 10006 条降为 41 条（30 errors / 11 warnings）；这主要是排除非源码噪声，不代表修复了一万项业务问题。

删除文件的原始内容备份在 `output/legacy-cleanup-backup/*.txt`，不参与应用构建。

## 保留的候选项

源码中没有引用的部分知识库工作流仍由 scripts 或测试使用，不能依据应用导入图删除。鉴权、旧数据解析、AI 输出修复与失败兜底保持不变。

资产和会话中的状态同步 effect、保存后整页刷新还可继续评估，但涉及表单、会话恢复和服务器数据一致性，需要交互回归后再改，不能作为简单死代码删除。

## 验证

- 生产构建及其中的 TypeScript 检查通过。
- 本次修改文件 ESLint 通过。
- 全量测试：1368 通过、43 跳过、2 失败。失败仍是前次已记录的背景透明度与聊天输入框样式断言；未为消除失败修改产品视觉。
- 未部署，未完成真实登录会话的端到端验收。不承诺删除未引用模块会减少生产包或带来可测的页面提速；反馈页减少一次数据库查询是明确的运行时优化。
